# Python environment lives outside the repo at C:\venvs\nanogpt
.venv/
__pycache__/
*.pyc
.ipynb_checkpoints/

# Raw run folders written by the notebook (their ZIPs included). Curated copies are committed under results/.
llm_runs/

# Scratch executions (setup checks)
scratch*.ipynb
